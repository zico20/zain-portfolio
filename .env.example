# ─────────────────────────────────────────────────────────────
# Public site URL — used for SEO metadata, OpenGraph, sitemap, robots.
# In production set this to your deployed origin, e.g. https://zain.dev
# ─────────────────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ─────────────────────────────────────────────────────────────
# Contact form delivery (OPTIONAL — not required to run locally).
# The /api/contact route works without these: it validates and logs
# the submission server-side. To actually deliver emails, sign up at
# https://resend.com, create an API key, and uncomment + fill these in,
# then wire them up inside src/app/api/contact/route.ts (see the TODO).
# ─────────────────────────────────────────────────────────────
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx
# CONTACT_TO_EMAIL=zainalmawla11@gmail.com
